/target
**/*.rs.bk

# On-demand vendored sources for offline builds (see README); not committed
# because the full cross-platform tree is ~240MB. Recreate with `cargo vendor`.
/vendor
/.cargo/config.toml

# Claude/superpowers working notes (not part of the project)
/docs/superpowers
/.claude-local
